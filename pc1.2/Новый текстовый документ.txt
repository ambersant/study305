 // Зчитування рядка зі стандартного введення
    /*cout << "Введіть текст: ";
    getline(cin, text);
 */
    // Обчислення кількості символів у рядку
    len = text.length();
    cout << "Кількість символів: " << len << endl;

    // Обчислення кількості біграм у рядку
    bigram_count = len - 1;
    cout << "Кількість біграм: " << bigram_count << endl;
   